{
  "name": "@ebms/shared",
  "version": "0.1.0",
  "private": true,
  "description": "EBMS Shared — TypeScript types, DTOs, and constants. No business logic.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write \"src/**/*.ts\""
  },
  "devDependencies": {
    "@ebms/config": "workspace:*"
  }
}
